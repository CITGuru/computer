/target
# Tracked, not ignored: this repository ships binaries. A release built from a
# tree that resolved differently from the run that checked it is not the build
# anybody tested.
*.gif
*.mp4
.env
/docs/internal/
.claude
*.png

# The README's animation is part of the README.
!media/demo.gif
.DS_Store