[package]
name = "computer-api"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "The HTTP contract a computer box is managed and driven over"
license = "MIT"

[dependencies]
computer-types = { path = "../computer-types", version = "0.1.0" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints.clippy]
string_slice = "deny"
